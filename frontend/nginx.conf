server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        # Si el archivo no existe, redirige al index.html de Angular
        try_files $uri $uri/ /index.html;
    }
}